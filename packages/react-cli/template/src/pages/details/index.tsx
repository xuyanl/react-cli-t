import { Outlet } from 'react-router-dom'

const Details = () => {
    return (
        <div>
            DetailsDetailsDetailsDetailsDetails
            <Outlet />
        </div>
    )
}

export default Details
